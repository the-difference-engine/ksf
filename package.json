{
  "name": "ks",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build-ts": "lerna run build-ts",
    "lint": "lerna run lint",
    "postinstall": "npm run bootstrap && npm run build-ts",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "formik": "^2.2.6",
    "jsonwebtoken": "^8.5.1",
    "yup": "^0.32.9"
  }
}
