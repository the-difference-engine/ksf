{
  "name": "ks",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build-ts": "lerna run build-ts",
    "lint": "lerna run lint",
    "postinstall": "npm run bootstrap && npm run build-ts",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "engines": {
    "node": ">=14.0.0 <=14.17.0",
    "npm": "<=6.14.13"
  }
}
